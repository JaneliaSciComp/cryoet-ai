"""Rendering and Neuroglancer helpers for the API.

Function-extracted from ``aicryoet-tools`` (decision §11.6 of the dashboard
MVP plan) — we copy only the specific functions the API needs so we don't
inherit napari/Qt/NiceGUI module-top imports. Treat these as our code from
now on: bug fixes won't auto-flow between repos.
"""
